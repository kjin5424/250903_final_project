<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mybatis.dao.IGroupMemberDAO">

	<select id="checkMemberGroup" resultType="java.lang.String">
		SELECT POSITION AS position
		FROM CURRENT_MEMBER_VIEW
		WHERE JOINCODE = (SELECT JOIN_CODE
						  FROM JOIN_TABLE
						  WHERE USER_CODE = #{userCode}
						    AND GROUP_APPLY_CODE = #{groupApplyCode})
		  AND GROUPAPPLYCODE = #{groupApplyCode}
	</select>

</mapper>

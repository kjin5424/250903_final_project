<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mybatis.ILoginDAO">

	<select id="findId" resultType="java.lang.String">
		SELECT USER_ID
		FROM USER_INFO
		WHERE USER_ID=#{userId}
	</select>

	<select id="loginProcess" resultType="java.lang.String">
		SELECT
		    USER_CODE
		FROM
		    USER_INFO A
		LEFT JOIN
		    USER_TABLE B ON A.USER_ID = B.USER_ID
		WHERE
		    A.USER_ID = #{userId}
		    AND CRYPTPACK.DECRYPT(A.PASSWORD, A.USER_ID) = #{password}
	</select>

</mapper>

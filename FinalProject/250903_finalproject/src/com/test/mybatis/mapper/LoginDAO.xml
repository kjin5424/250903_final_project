<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mybatis.ILoginDAO">

	<select id="loginProcess" resultType="com.test.mybatis.UserDTO">
		SELECT
		    A.NICKNAME AS nickName
		    , B.USER_CODE AS userCode
		FROM
		    USER_INFO A
		INNER JOIN
		    USER_TABLE B ON A.USER_ID = B.USER_ID
		WHERE
		    A.USER_ID = #{userId}
		    AND CRYPTPACK.DECRYPT(A.PASSWORD, A.USER_ID) = #{password}
	</select>

</mapper>
